<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ImplicitIntents">
        <activity
            android:name=".menus.OrderActivity"
            android:exported="false" />
        <activity
            android:name=".menus.MenusActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".date_picker_dialog.DatePickerDialogActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".alert_dialog.AlertDialogActivity"
            android:exported="true" />
    </application>

</manifest>
